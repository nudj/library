const GENERIC = 'GENERIC'

// validation
const ALREADY_EXISTS = 'ALREADY_EXISTS'
const INVALID = 'INVALID'

const statusCodes = {
  BAD_REQUEST: 400
}

module.exports = {
  GENERIC,
  INVALID,
  ALREADY_EXISTS,
  statusCodes
}
